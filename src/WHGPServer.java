import java.net.ServerSocket;
import java.util.HashSet;
import java.util.Set;

public class WHGPServer {
    private ServerSocket serverSocket;

    private static Set<Player> players = new HashSet<>();
}
